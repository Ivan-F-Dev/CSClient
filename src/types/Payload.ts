export type payloadForReg = {
    name: string
    surname: string
    dateOfBirth: string
    login: string
    password: string
}
export type payloadForLog = {
    login: string
    password: string
}